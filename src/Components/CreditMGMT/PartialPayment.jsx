import React from "react";

function PartialPayment() {
  // progress bar on item registrations is not being shown
  return <div>PartialPayment follow up system has to be done</div>;
}

export default PartialPayment;
